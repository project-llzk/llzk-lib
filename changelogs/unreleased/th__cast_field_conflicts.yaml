added:
  - Verification that `unifiable_cast` does not change a field-specified felt value to unspecified.
  - Verification that `unifiable_cast` does not cast the same unspecified field value to two different specified fields.
