fixed:
  - Evaluate template expressions used by a target once their value and type dependencies become concrete, preserving normalized expressions across partial specialization.
  - Specialize expression-only free functions and structs, including unparameterized and explicitly empty struct argument forms, and retry temporarily blocked expressions at the next fixpoint iteration.
  - Preserve concrete template bindings in rebuilt nested calls and in array, struct, POD, and scalar type arguments.
  - Treat equivalent felt spellings as the same specialization value for function and struct instantiation, and discard failed prospective specializations without leaking generated definitions or warnings.
