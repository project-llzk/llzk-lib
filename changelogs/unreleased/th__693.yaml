added:
  - "`-llzk-const-global-propagation` pass to replace uses of `const` `global.def` symbols with constant values"

changed:
  - "`global.def` verification now requires the type(s) in the optional initializer attribute to match the declared type of the `global.def` symbol"
