fixed:
  - Preserve value-copy semantics when scalarizing POD records containing nested PODs or statically shaped arrays, including across SCF lifting and virtual POD storage; reject unsupported mutable copies, reuse payload-free shape arrays, and treat struct values as identity-bearing handles that continue to reference the same component.
  - Scalarize uninitialized nested POD records while preserving independent read snapshots. Classify copy support consistently at every nesting depth using concrete immutable and handle types, and diagnose unsupported leaves, array payloads, and affine-map POD instantiations without constructing invalid IR.
