fixed:
  - Prevent `--cse` from merging distinct `array.new` allocations.
added:
  - Add `--llzk-remove-unused-discardable-allocations` for removing unread discardable array allocations and their dead stores.
