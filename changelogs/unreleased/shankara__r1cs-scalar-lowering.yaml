fixed:
  - Lower POD values carried by `scf.while` to scalar SSA values while preserving supported loop semantics and safely rejecting asymmetric or unsupported loop shapes.
  - Preserve negative `felt` constants when canonicalizing `cast.toindex` instead of reinterpreting them as nonnegative indices.
