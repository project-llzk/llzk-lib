fixed:
  - Lower POD values carried by `scf.while` to scalar SSA values when their storage is a fresh, exclusively owned tree, safely rejecting shared storage, unknown provenance, and asymmetric or unsupported loop shapes.
  - Preserve negative `felt` constants when canonicalizing `cast.toindex` instead of reinterpreting them as nonnegative indices.
