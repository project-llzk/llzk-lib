fixed:
  - Lower scalar leaves of POD values carried by `scf.while`, including read-only nested copies, with conservative initialization checks. Reject mutations of nested copies, mutable aggregate leaves, unknown provenance, and asymmetric or unsupported loop shapes rather than erasing aggregate copy boundaries.
  - Preserve negative `felt` constants when canonicalizing `cast.toindex` instead of reinterpreting them as nonnegative indices.
