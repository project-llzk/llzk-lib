// RUN: not llzk-opt %s 2>&1 | FileCheck %s

// ----- invalid-key.mlir
module {
  smt.solver () : () -> () {
    smt_info.set "status" sat
    smt.yield
  }
}

// CHECK: error: custom op 'smt_info.set' keyword must start with ':'
